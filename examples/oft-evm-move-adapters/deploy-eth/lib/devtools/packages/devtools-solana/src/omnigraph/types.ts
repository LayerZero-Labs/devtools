import type { IOmniSDK as IOmniSDKAbstract } from '@layerzerolabs/devtools'

/**
 * Base interface for all Solana SDKs, adding the Solana specific attributes
 */
export interface IOmniSDK extends IOmniSDKAbstract {}
