/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bool, bytes, mapSerializer, string, struct, u32 } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type QuoteInstructionAccounts = {
    store: PublicKey | Pda
    endpoint: PublicKey | Pda
}

// Data.
export type QuoteInstructionData = {
    discriminator: Uint8Array
    dstEid: number
    receiver: Uint8Array
    message: string
    options: Uint8Array
    payInLzToken: boolean
}

export type QuoteInstructionDataArgs = {
    dstEid: number
    receiver: Uint8Array
    message: string
    options: Uint8Array
    payInLzToken: boolean
}

export function getQuoteInstructionDataSerializer(): Serializer<QuoteInstructionDataArgs, QuoteInstructionData> {
    return mapSerializer<QuoteInstructionDataArgs, any, QuoteInstructionData>(
        struct<QuoteInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['dstEid', u32()],
                ['receiver', bytes({ size: 32 })],
                ['message', string()],
                ['options', bytes({ size: u32() })],
                ['payInLzToken', bool()],
            ],
            { description: 'QuoteInstructionData' }
        ),
        (value) => ({
            ...value,
            discriminator: new Uint8Array([149, 42, 109, 247, 134, 146, 213, 123]),
        })
    ) as Serializer<QuoteInstructionDataArgs, QuoteInstructionData>
}

// Args.
export type QuoteInstructionArgs = QuoteInstructionDataArgs

// Instruction.
export function quote(
    context: Pick<Context, 'programs'>,
    input: QuoteInstructionAccounts & QuoteInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('myoapp', 'HFyiETGKEUS9tr87K1HXmVJHkqQRtw8wShRNTMkKKxay')

    // Accounts.
    const resolvedAccounts = {
        store: {
            index: 0,
            isWritable: false as boolean,
            value: input.store ?? null,
        },
        endpoint: {
            index: 1,
            isWritable: false as boolean,
            value: input.endpoint ?? null,
        },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: QuoteInstructionArgs = { ...input }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getQuoteInstructionDataSerializer().serialize(resolvedArgs as QuoteInstructionDataArgs)

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
