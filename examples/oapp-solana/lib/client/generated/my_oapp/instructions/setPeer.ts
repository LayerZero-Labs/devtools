/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bytes, mapSerializer, struct, u32 } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type SetPeerInstructionAccounts = {
    admin: Signer
    peer: PublicKey | Pda
    store: PublicKey | Pda
    systemProgram?: PublicKey | Pda
}

// Data.
export type SetPeerInstructionData = {
    discriminator: Uint8Array
    remoteEid: number
    peerAddress: Uint8Array
}

export type SetPeerInstructionDataArgs = {
    remoteEid: number
    peerAddress: Uint8Array
}

export function getSetPeerInstructionDataSerializer(): Serializer<SetPeerInstructionDataArgs, SetPeerInstructionData> {
    return mapSerializer<SetPeerInstructionDataArgs, any, SetPeerInstructionData>(
        struct<SetPeerInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['remoteEid', u32()],
                ['peerAddress', bytes({ size: 32 })],
            ],
            { description: 'SetPeerInstructionData' }
        ),
        (value) => ({
            ...value,
            discriminator: new Uint8Array([32, 70, 184, 229, 200, 115, 227, 177]),
        })
    ) as Serializer<SetPeerInstructionDataArgs, SetPeerInstructionData>
}

// Args.
export type SetPeerInstructionArgs = SetPeerInstructionDataArgs

// Instruction.
export function setPeer(
    context: Pick<Context, 'programs'>,
    input: SetPeerInstructionAccounts & SetPeerInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('myoapp', 'HFyiETGKEUS9tr87K1HXmVJHkqQRtw8wShRNTMkKKxay')

    // Accounts.
    const resolvedAccounts = {
        admin: {
            index: 0,
            isWritable: true as boolean,
            value: input.admin ?? null,
        },
        peer: { index: 1, isWritable: true as boolean, value: input.peer ?? null },
        store: {
            index: 2,
            isWritable: false as boolean,
            value: input.store ?? null,
        },
        systemProgram: {
            index: 3,
            isWritable: false as boolean,
            value: input.systemProgram ?? null,
        },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: SetPeerInstructionArgs = { ...input }

    // Default values.
    if (!resolvedAccounts.systemProgram.value) {
        resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
        )
        resolvedAccounts.systemProgram.isWritable = false
    }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getSetPeerInstructionDataSerializer().serialize(resolvedArgs as SetPeerInstructionDataArgs)

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
