/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import { Context, Pda, PublicKey, Signer, TransactionBuilder, transactionBuilder } from '@metaplex-foundation/umi'
import { Serializer, bool, bytes, mapSerializer, struct } from '@metaplex-foundation/umi/serializers'
import { ResolvedAccount, ResolvedAccountsWithIndices, getAccountMetasAndSigners } from '../shared'

// Accounts.
export type SetOrderedNonceInstructionAccounts = {
    admin: Signer
    store: PublicKey | Pda
}

// Data.
export type SetOrderedNonceInstructionData = {
    discriminator: Uint8Array
    orderedNonce: boolean
}

export type SetOrderedNonceInstructionDataArgs = { orderedNonce: boolean }

export function getSetOrderedNonceInstructionDataSerializer(): Serializer<
    SetOrderedNonceInstructionDataArgs,
    SetOrderedNonceInstructionData
> {
    return mapSerializer<SetOrderedNonceInstructionDataArgs, any, SetOrderedNonceInstructionData>(
        struct<SetOrderedNonceInstructionData>(
            [
                ['discriminator', bytes({ size: 8 })],
                ['orderedNonce', bool()],
            ],
            { description: 'SetOrderedNonceInstructionData' }
        ),
        (value) => ({
            ...value,
            discriminator: new Uint8Array([241, 250, 152, 143, 162, 40, 143, 242]),
        })
    ) as Serializer<SetOrderedNonceInstructionDataArgs, SetOrderedNonceInstructionData>
}

// Args.
export type SetOrderedNonceInstructionArgs = SetOrderedNonceInstructionDataArgs

// Instruction.
export function setOrderedNonce(
    context: Pick<Context, 'programs'>,
    input: SetOrderedNonceInstructionAccounts & SetOrderedNonceInstructionArgs
): TransactionBuilder {
    // Program ID.
    const programId = context.programs.getPublicKey('omnicounter', 'HFyiETGKEUS9tr87K1HXmVJHkqQRtw8wShRNTMkKKxay')

    // Accounts.
    const resolvedAccounts = {
        admin: {
            index: 0,
            isWritable: false as boolean,
            value: input.admin ?? null,
        },
        store: {
            index: 1,
            isWritable: true as boolean,
            value: input.store ?? null,
        },
    } satisfies ResolvedAccountsWithIndices

    // Arguments.
    const resolvedArgs: SetOrderedNonceInstructionArgs = { ...input }

    // Accounts in order.
    const orderedAccounts: ResolvedAccount[] = Object.values(resolvedAccounts).sort((a, b) => a.index - b.index)

    // Keys and Signers.
    const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, 'programId', programId)

    // Data.
    const data = getSetOrderedNonceInstructionDataSerializer().serialize(
        resolvedArgs as SetOrderedNonceInstructionDataArgs
    )

    // Bytes Created On Chain.
    const bytesCreatedOnChain = 0

    return transactionBuilder([{ instruction: { keys, programId, data }, signers, bytesCreatedOnChain }])
}
