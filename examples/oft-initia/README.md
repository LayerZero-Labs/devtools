install go
```
brew install go
```
install initiad
```bash
git clone https://github.com/initia-labs/initia
cd initia
make install
which initiad

echo 'export PATH="$PATH:<your-path-to-go-bin>"' >> ~/.zshrc
source ~/.zshrc
```
