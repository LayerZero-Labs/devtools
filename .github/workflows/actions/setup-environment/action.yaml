name: Setup environment
description: Setup node & package manager, checkout code
runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v4
      name: Install pnpm
      with:
        version: 8.15.6
        run_install: false

